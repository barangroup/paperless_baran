var db = require('mongo_schemas');

module.exports = function(sms_res) {
  if (sms_res) {
    sms_res.to = "";
    sms_res.to = sms_res.receptor;
    sms_res.date = new Date();

    new db.send_sms_archive(sms_res).save(function(err) {
      if (err) console.log(err);
    });
  }
};
